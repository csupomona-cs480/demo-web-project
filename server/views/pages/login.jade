extends ../layouts/layout

block content
  .row
    .col-md-12
      h1 Login:
      div
        p 
          a.btn.btn-warning(href='/local') Login with local account
        p
          a.btn.btn-warning(href="/auth/facebook") Login with Facebook
      br
      if (messages.error)
        p
          span(style="color:red")= messages.error
