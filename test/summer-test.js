var test = require('tape')
var summer = require('../')

test('summer', function (t) {
  
     t.doesNotThrow(function () {
                    app.use(passport.initialize());
                    });
    
  
  t.end()
     });
